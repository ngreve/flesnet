#!/bin/bash

set -e
shopt -u nocaseglob
LANG=C

DIR="$(git rev-parse --show-toplevel)"
cd "$DIR"

clang-format-22 -style=file -fallback-style=none -i \
  lib/{cri,fles_ipc,fles_tools,logging,monitoring,pda,shm_ipc,tsb_ucx}/*.[hc]pp \
  app/*/*.[hc]pp \
  test/*.[hc]pp
